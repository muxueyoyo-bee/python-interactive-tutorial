# ?977?: 编写上下文管理器: YoutubeDL

## ????

上下文管理器（Context Manager）用 with 语句管理资源的获取和释放。

源文件 YoutubeDL.py 定义了类 \

## ???

实现 __enter__(self) 返回 self，__exit__(self, exc_type, exc_val, exc_tb) 处理清理

## ????

- `with` ?????????? `__exit__`???????
- `__exit__` ?? `True` ????????????
- ???? `contextlib.contextmanager` ??? + `yield` ??

??????????????
