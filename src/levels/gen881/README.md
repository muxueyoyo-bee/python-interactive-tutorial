# ?881?: 编写装饰器: register_operation

## ????

装饰器是 Python 中用于包装函数、添加横切关注点的强大模式。

源文件 base.py（sqlalchemy/alembic）中 \

## ???

外层函数接受 func 参数，内层定义 wrapper(*args, **kwargs)，外层 return wrapper

## ????

- ?????? `decorator(func)` ?????
- ?? `wrapper` ? `*args, **kwargs` ??????
- `functools.wraps(func)` ?????? `__name__` ? `__doc__`

??????????????
